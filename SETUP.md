# SETUP — 残タスク・検討事項

最終更新: 2026-09-12（A章 完了）
本番サイト: https://decode-slang.com
リポジトリ: https://github.com/nsvmm-dev/sec-edgar-insider-tracker
ロゴ・コンセプトボード: https://claude.ai/code/artifact/89bae752-8c53-4756-a44b-5c514133e4dc

> このファイルはタスク管理用のメモ。実装の詳細は `README.md` / `CLAUDE.md` / `A_sec-edgar-insider-tracker_spec.md` を参照。

---

## A. すぐにやること

- [x] **A-1. ロゴ・コンセプトボードの更新**
  EdgarHawk のタカのアイコンで作り直し、同じ URL に再公開済み。（担当: Claude）

- [x] **A-2. Dependabot PR #7（Astro 4→7.3.1）の後始末**
  クローズ済みを確認（Dependabot が重複を検知して自動クローズ）。（担当: あなた）

- [x] **A-3. Cloudflare Web Analytics でデータが実際に入っているか確認**
  4件のPVを確認 — 正常に計測されている。（担当: あなた）

- [x] **A-4. README のセットアップ手順に Node.js バージョン要件を明記**
  `README.md` と `site/README.md` の両方に追記済み。（担当: Claude）

### A-2 詳細手順：Dependabot PR #7 のクローズ

1. https://github.com/nsvmm-dev/sec-edgar-insider-tracker/pull/7 を開く
2. すでに **Closed**（自動クローズ済み）になっていれば、何もしなくてOK — Dependabot は、提案していた更新が base ブランチ（main）に既に反映されていることを検知すると自動でクローズすることがあります
3. まだ **Open** のままなら：
   - ページ内に "This branch is out-of-date" や "Nothing to compare" 等の表示が出ているはずです（main 側で既に Astro 7 化が完了しているため）
   - ページ最下部までスクロールし、グレーの「**Close pull request**」ボタンをクリック
   - ⚠️ **「Merge」は押さないでください**。このPRは単純なバージョン番号の変更のみで、実際に必要だった付随修正（`content.config.ts` への移行、Node.js 22 化など）を含んでいないため、マージすると壊れたコードに戻る可能性があります
4. コメントは不要、クローズするだけでOK

### A-3 詳細手順：Cloudflare Web Analytics のデータ確認

1. Cloudflare ダッシュボード → 左メニュー「**Analytics**」→「**Web analytics**」（アカウント直下のメニュー。特定ドメインの中ではない方）
2. サイト一覧に `decode-slang.com` が表示されているはずなので、**歯車アイコン（設定）ではなく、サイト名自体をクリック**してアナリティクス画面を開く
3. 右上あたりの期間セレクター（Last 24 hours / Last 7 days 等）を確認
4. 「Page views」「Visits」のグラフ・数値が出ているか確認
   - **0件でも慌てないでください。** よくある原因：
     - ビーコン設置後まだ十分な時間が経っていない
     - **自分のブラウザに広告ブロッカーやプライバシー拡張機能が入っていると、Cloudflare のアナリティクスビーコン自体がブロックされ、自分のアクセスがカウントされないことがあります**
   - 確認方法：拡張機能を無効にしたブラウザ、またはシークレット/プライベートウィンドウで `https://decode-slang.com/` を数ページ閲覧 → 1〜2分待ってからこの画面をリロード → PV が反映されるか確認

---

## B. 継続監視（作業不要・時間経過で分かるもの）

- [ ] **Phase 2 の1週間無人稼働確認**：平日の daily pipeline・土曜の weekly summary が今後もエラーなく回り続けるか（仕様§10の完了基準）
- [ ] **Google Search Console のインデックス状況**：数日〜1-2週間でクロール・掲載状況を確認
- [ ] **Dependabot PR は今後も定期的に出続ける**：都度 CI green を確認してマージ、メジャー更新だけは都度相談

## C. コンテンツ拡張（要判断・未着手）

| 項目 | 状況 |
|---|---|
| 仕様テンプレート3（IPOウォッチ／S-1要約） | 未着手。次に作るか保留か要判断 |
| 銘柄別ページ（`/ticker/AAPL/` に該当社の全ブリーフ） | 未着手。SEO・内部リンクに有効 |
| 記事本文に事実テーブルを追加するか | 検討のみ（現状3段落の文章のみ） |
| OG画像（SNSシェア時のサムネイル） | 未着手。今は favicon のみ |
| PWA/ブックマーク用の PNG favicon フォールバック | 未着手。現状 SVG のみ |

## D. Phase 3（SNS・ニュースレター）— 未着手

- `social_agent.py` / `newsletter_agent.py` は下書き専用で存在するが、日次/週次ワークフローに未組み込み
- 有効化には X アカウント・Beehiiv アカウントが必要（保有状況未確認）
- 組み込む場合も「下書き生成のみ自動化、投稿は手動」の運用から開始する想定

## E. マネタイズ準備（Phase 4・未着手）

- アフィリエイト：対象証券会社の選定・プログラム登録（サイドバー/フッター限定、本文には入れない方針は確定済み — 仕様§8）
- 広告：AdSense 等のアカウント取得、枠の実装（フラグで非表示にしておき、アカウント取得後に有効化する形が可能）
- 広告/トラッキング Cookie を使う場合のみ、EU向け同意バナー（CMP）が必要になる

## F. 確定済み・対応不要（参考）

- [x] 運営主体：個人運営、法人化予定なし
- [x] 訂正・削除ポリシー：現状の方針でOK
- [x] ドメイン：`decode-slang.com` のまま（ブランド名との不一致は許容済み）
- [x] ブランド名・ロゴ：EdgarHawk に確定。簡易な商標衝突チェック済み（正式な商標クリアランス検索ではない旨は留意）
- [x] Node.js バージョン起因のデプロイ不具合：修正済み（全ワークフロー Node 22 化）
